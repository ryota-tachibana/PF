<% if resource.errors.any? %>
  <div id="error_explanation">
    <h3>登録できませんでした。以下対応してください。</h3>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
